podTemplate(
label: 'sdcclient',
cloud: 'openshift',
containers: [
    containerTemplate(name: 'sdcclient',
                    image: 'stewartshea/sysdig-sdcclient:0.1',
                    resourceLimitMemory: '512Mi',
                    workingDir:'/home/jenkins'
                    )
    ]
    )
{

    node('sdcclient') {
    container('sdcclient')
    {
        stage('Test agent') {
                sh 'echo HELLO WORLD'
        
        }
    }
    }
}