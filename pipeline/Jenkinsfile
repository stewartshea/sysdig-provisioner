podTemplate(
label: 'sdcclient',
cloud: 'openshift',
containers: [
    containerTemplate(name: 'sdcclient',
                    image: 'stewartshea/sysdig-sdcclient:0.3',
                    resourceLimitMemory: '512Mi',
                    workingDir:'/home/jenkins'
                    )
    ]
    )
{

    node('sdcclient') {
    container('sdcclient')
    {
        environment {
            SYSDIG_API_TOKEN = credentials('sysdig-api-token')
                }
        stage('Test agent') {
                sh 'cat $SYSDIG_API_TOKEN > tokentest'
        
        }
    }
    }
}